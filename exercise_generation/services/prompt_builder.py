import json


class ExercisePromptBuilder:
    """
    مسؤول عن بناء التعليمات التي سترسل إلى الذكاء الاصطناعي.
    """

    @staticmethod
    def build(
        axis,
        difficulty: str,
        exercise_type: str,
        skill: str,
        include_solution: bool,
    ) -> str:
        course_content = json.dumps(
            axis.content,
            ensure_ascii=False,
            indent=2,
        )

        skill_instruction = (
            skill.strip()
            if skill and skill.strip()
            else "اختر مهارة مرتبطة مباشرة بمحتوى الدرس."
        )

        if include_solution:
            solution_instruction = """
يجب إنشاء حل كامل وبسيط يتضمن:

- الفكرة العامة للحل.
- شرحًا واضحًا يناسب تلميذًا ضعيف المستوى.
- خطوات مرتبة لا تتجاوز خمس خطوات.
- العمليات الرياضية مكتوبة بصيغة LaTeX.
- جوابًا نهائيًا واضحًا.
- التحقق من النتيجة.
- الأخطاء الشائعة.
- ثلاثة تلميحات متدرجة عند الحاجة.
"""
        else:
            solution_instruction = """
لا تنشئ حلًا للتمرين.
ضع القيمة التالية في solution:

"solution": {}
"""

        return f"""
أنت أستاذ رياضيات خبير في منهاج السنة الثالثة ثانوي
وفي البكالوريا الجزائرية.

نعمل على منصة تعليمية جزائرية هدفها مساعدة التلميذ
على فهم الرياضيات بطريقة بسيطة ومنظمة.

مهمتك هي إنشاء تمرين واحد فقط اعتمادًا على محتوى
الدرس الموجود في المحور التالي.

معلومات المحور:

معرف المحور:
{axis.id}

وسم المحور:
{axis.tag}

عنوان المحور:
{axis.title}

محتوى الدرس:

{course_content}

إعدادات التمرين:

مستوى الصعوبة:
{difficulty}

نوع التمرين:
{exercise_type}

المهارة المطلوبة:
{skill_instruction}

القواعد الإجبارية:

1. أنشئ تمرينًا واحدًا فقط.

2. يجب أن يكون التمرين مرتبطًا مباشرة بمحتوى الدرس.

3. لا تستعمل مفاهيم غير موجودة داخل الدرس.

4. يجب أن يكون السؤال مستقلاً بنسبة 100%.

5. لا تجعل السؤال يعتمد على نتيجة سؤال سابق.

6. ضع جميع المعطيات الضرورية داخل standalone_text.

7. يجب أن تكون depends_on قائمة فارغة دائمًا.

8. استعمل لغة عربية واضحة ومناسبة لتلميذ
   السنة الثالثة ثانوي في الجزائر.

9. تجنب اللغة الجامعية المعقدة.

10. حافظ على الرموز الرياضية مثل:
    u_n و v_n و w_n.

11. اكتب التعبيرات الرياضية داخل:
    \\( ... \\)

12. لا تكتب رموز LaTeX متلاصقة بطريقة غير واضحة.

مثال خاطئ:

\\(u_1u_2u_3\\)

مثال صحيح:

\\(u_1 \\times u_2 \\times u_3\\)

13. لا تكتب أي شرح خارج JSON.

14. لا تستعمل Markdown.

15. لا تضع JSON داخل ```json.

16. يجب أن تكون الإجابة JSON صالحًا فقط.

{solution_instruction}

البنية المطلوبة حرفيًا:

{{
  "exercise": {{
    "title": "عنوان واضح للتمرين",
    "text": "السؤال الرئيسي",
    "standalone_text": "نص كامل يحتوي على جميع المعطيات والسؤال",
    "context": "المعطيات الرياضية الضرورية",
    "difficulty": "{difficulty}",
    "question_type": "{exercise_type}",
    "skill": "المهارة التي يتدرب عليها التلميذ",
    "is_standalone": true,
    "depends_on": [],
    "images": [],
    "solution": {{
      "strategy": "الفكرة العامة المستعملة في الحل",
      "detailed_explanation": "شرح بسيط ومفهوم للحل",
      "steps": [
        {{
          "order": 1,
          "title": "عنوان الخطوة",
          "explanation": "ماذا نفعل في هذه الخطوة ولماذا",
          "calculation": "\\\\(العملية الرياضية\\\\)",
          "result": "\\\\(النتيجة\\\\)"
        }}
      ],
      "final_answer": "الجواب النهائي بوضوح",
      "verification": "طريقة بسيطة للتحقق من الجواب",
      "common_mistakes": [
        {{
          "mistake": "الخطأ الذي قد يقع فيه التلميذ",
          "correction": "كيفية تصحيح الخطأ"
        }}
      ],
      "hints": [
        {{
          "level": 1,
          "hint": "تلميح بسيط لا يعطي الحل مباشرة"
        }},
        {{
          "level": 2,
          "hint": "تلميح أكثر وضوحًا"
        }},
        {{
          "level": 3,
          "hint": "تلميح قريب من بداية الحل"
        }}
      ],
      "alternative_method": "",
      "is_complete": true
    }}
  }}
}}

أعد JSON فقط.
"""